# Ubuntu Chroot GUI Installer for Termux

## 🖥️ رابط گرافیکی نصب اوبونتو روی ترماکس

یک رابط گرافیکی کامل و آسان برای نصب اوبونتو chroot روی ترماکس با LXDE دسکتاپ و VNC سرور.

### 📱 سازگاری:
- ✅ گوشی Poco X3 Pro
- ✅ سایر دستگاه‌های اندروید
- ✅ نیاز به روت (توصیه می‌شود)
- ✅ حداقل 4GB فضای خالی

## 🚀 نصب سریع:

```bash
# در ترماکس
git clone https://github.com/amirmsoud16/ubuntu-chroot-pk-.git
cd ubuntu-chroot-pk-
chmod +x run_gui_installer.sh
./run_gui_installer.sh
```

یا دانلود مستقیم:
```bash
# در ترماکس
wget https://raw.githubusercontent.com/amirmsoud16/ubuntu-chroot-pk-/main/ubuntu_chroot_gui.py
wget https://raw.githubusercontent.com/amirmsoud16/ubuntu-chroot-pk-/main/run_gui_installer.sh
chmod +x run_gui_installer.sh
./run_gui_installer.sh
```

## 📋 فایل‌های پروژه:

### فایل‌های اصلی:
- **`ubuntu_chroot_gui.py`** - رابط گرافیکی اصلی
- **`run_gui_installer.sh`** - اسکریپت اجرای آسان
- **`GUI_README.md`** - راهنمای کامل رابط گرافیکی
- **`README.md`** - این فایل
- **`LICENSE`** - لایسنس MIT
- **`.gitignore`** - فایل‌های نادیده گرفته شده

## 🎯 ویژگی‌های رابط گرافیکی:

### 4 گزینه اصلی:
1. **بررسی سیستم** ✅ - بررسی کامل پیش‌نیازها و اطلاعات دستگاه
2. **نصب اوبونتو** 📦 - نصب با انتخاب نوع (Chroot/Proot) و نسخه
3. **حذف کامل اوبونتو** 🗑️ - حذف کامل و تمیز
4. **خروج** 🚪 - خروج از برنامه

### ویژگی‌های فنی:
- ✅ رابط کاربری مدرن و تیره
- ✅ تشخیص خودکار اطلاعات دستگاه
- ✅ انتخاب نوع نصب (Chroot/Proot)
- ✅ انتخاب نسخه اوبونتو (18.04, 20.04, 22.04, 24.04)
- ✅ نمایش پیشرفت عملیات
- ✅ مدیریت خطا و عیب‌یابی
- ✅ عملیات همزمان (بدون قفل شدن)
- ✅ رنگ‌بندی مناسب برای وضعیت‌ها
- ✅ لاگ رنگی با timestamp

## 📊 اطلاعات نمایش داده شده:

### بررسی سیستم:
- ✓/✗ محیط ترماکس
- ✓/✗ دسترسی روت
- اطلاعات کامل دستگاه (معماری، مدل، اندروید، رم)
- فضای دیسک موجود
- ✓/✗ اتصال اینترنت
- لیست پکیج‌های نصب شده/نشده
- وضعیت نصب اوبونتو

### نصب:
- انتخاب نوع نصب (Chroot/Proot)
- انتخاب نسخه اوبونتو (18.04, 20.04, 22.04, 24.04)
- پیشرفت هر مرحله
- زمان تخمینی باقی‌مانده
- پیام‌های موفقیت/خطا



## 🎨 رنگ‌بندی:

- **سبز** ✅ - موفقیت
- **قرمز** ❌ - خطا
- **نارنجی** ⚠️ - هشدار
- **آبی** ℹ️ - اطلاعات

## 🔧 عیب‌یابی:

### مشکل: خطای "couldn't connect to display"
این مشکل در Termux رایج است. راه‌حل‌ها:

#### راه‌حل 1: استفاده از حالت Console
```bash
# اجرای نسخه console
python3 ubuntu_chroot_console.py
```

#### راه‌حل 2: تنظیم VNC
```bash
# نصب VNC server
pkg install tigervnc -y

# شروع VNC server
vncserver :1 -geometry 1280x720 -depth 24

# تنظیم DISPLAY
export DISPLAY=:1

# اجرای GUI
python3 ubuntu_chroot_gui.py
```

#### راه‌حل 3: نصب tkinter
```bash
# نصب tkinter
pkg install python-tkinter -y

# بررسی نصب
python3 -c "import tkinter; print('tkinter installed')"
```

### مشکل: رابط گرافیکی باز نمی‌شود
```bash
# نصب tkinter
pkg install python-tkinter -y

# بررسی نصب
python3 -c "import tkinter; print('tkinter installed')"
```

### مشکل: خطای "No display"
```bash
# تنظیم DISPLAY
export DISPLAY=:0

# یا استفاده از VNC
vncserver :1
export DISPLAY=:1
```

### مشکل: فضای دیسک کم
```bash
# بررسی فضای دیسک
df -h

# پاک کردن فایل‌های غیرضروری
pkg clean
```

## 📝 مراحل استفاده:

### مرحله 1: آماده‌سازی
```bash
# نصب پایتون و tkinter
pkg install python python-tkinter -y

# نصب پکیج‌های مورد نیاز
pkg install wget curl proot tar -y
```

### مرحله 2: اجرای رابط گرافیکی
```bash
# دانلود و اجرا
git clone https://github.com/amirmsoud16/ubuntu-chroot-pk-.git
cd ubuntu-chroot-pk-
chmod +x run_gui_installer.sh
./run_gui_installer.sh
```

### مرحله 3: استفاده از منو
1. **بررسی سیستم** - ابتدا سیستم را بررسی کنید
2. **نصب اوبونتو** - نوع نصب و نسخه را انتخاب کنید
3. **حذف اوبونتو** - در صورت نیاز به حذف
4. **خروج** - برای خروج از برنامه

### مرحله 4: انتخاب نصب
- **نوع نصب**: Chroot (برای دستگاه‌های روت) یا Proot (برای دستگاه‌های غیر روت)
- **نسخه اوبونتو**: 18.04, 20.04, 22.04, یا 24.04
- **تأیید نصب**: بررسی نهایی قبل از شروع

## 🎯 مزایای رابط گرافیکی:

1. **سادگی استفاده** - بدون نیاز به دستورات پیچیده
2. **نمایش بصری** - دیدن پیشرفت عملیات
3. **مدیریت خطا** - نمایش واضح مشکلات
4. **عملیات امن** - تأیید قبل از حذف
5. **رابط فارسی** - مناسب کاربران فارسی‌زبان

## 📋 پیش‌نیازها:

- ✅ دستگاه روت شده (توصیه می‌شود)
- ✅ حداقل 4GB فضای خالی
- ✅ اتصال اینترنت پایدار
- ✅ ترماکس نصب شده

## ⏱️ زمان نصب:

- **بررسی سیستم**: 1-2 دقیقه
- **نصب اوبونتو**: 10-20 دقیقه (بسته به سرعت اینترنت)
- **نصب LXDE**: 5-10 دقیقه
- **تنظیم VNC**: 1-2 دقیقه

## 🔒 امنیت:

- تأیید قبل از حذف کامل
- بررسی پیش‌نیازها قبل از نصب
- نمایش واضح عملیات‌های حساس
- امکان لغو عملیات

## 📞 پشتیبانی:

اگر مشکلی داشتید:
1. فایل‌های log را بررسی کنید
2. دستورات را دوباره اجرا کنید
3. فضای دیسک را بررسی کنید
4. اتصال اینترنت را تست کنید

---

**نکته**: این پروژه برای گوشی Poco X3 Pro و سایر دستگاه‌های اندروید طراحی شده است.

## 📄 لایسنس:

این پروژه تحت لایسنس MIT منتشر شده است. برای جزئیات بیشتر فایل `LICENSE` را مطالعه کنید.

## 🤝 مشارکت:

مشارکت‌های شما در بهبود این پروژه بسیار ارزشمند است! لطفاً:

1. این repository را fork کنید: https://github.com/amirmsoud16/ubuntu-chroot-pk-
2. یک branch جدید برای feature خود ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. تغییرات خود را commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. به branch خود push کنید (`git push origin feature/AmazingFeature`)
5. یک Pull Request ایجاد کنید

## ⭐ ستاره‌دهی:

اگر این پروژه برای شما مفید بود، لطفاً یک ستاره ⭐ به آن بدهید! 